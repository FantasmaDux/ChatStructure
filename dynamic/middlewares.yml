#http:
#  middlewares:
#    strip-auth-prefix:
#      stripPrefix:
#        prefixes:
#          - /auth
#
#    strip-networking-prefix:
#      stripPrefix:
#        prefixes:
#          - /networking

http:
  middlewares:
    strip-kafka-ui-prefix:
      stripPrefix:
        prefixes:
          - "/kafka-ui"